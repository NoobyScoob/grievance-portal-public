import React from 'react';
import { withStyles } from '@material-ui/core';

const styles = ({

});

const Info = (props) => {
    return(
        <div>
        </div>
    );
}

export default withStyles(styles)(Info);